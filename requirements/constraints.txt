# Pip constraints (used with: pip install -r requirements/local.txt -c requirements/constraints.txt)
# Keep in sync with requirements/base.txt — upgrade Django to 5.2+ before loosening these.
Django>=5.0,<5.2
django-filter>=24.0,<25
# mypy stubs: 3.17+ / django-stubs 6 need Django 5.2+
django-stubs==5.2.9
djangorestframework-stubs==3.16.9
